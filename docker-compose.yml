version: "3.4"
services:
  speos-rpc:
    image: ghcr.io/ansys-internal/speos-rpc:2025.1.1.26391
    container_name: speos-rpc
    ports:
      - "50098:50098"
    environment:
      - SPEOS_LOG_LEVEL=2
      - ANSYSLMD_LICENSE_FILE=$LICENSE_SERVER
    entrypoint: /app/SpeosRPC_Server.x
    volumes:
      - type: bind
        source: ./tests/assets
        target: /app/assets
        read_only: false
